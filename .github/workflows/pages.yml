name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

# Do not cancel in-progress runs: a new push could cancel build before upload, leaving deploy with no artifact.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Use npm 11 for npm ci
        run: npm install -g npm@11

      - name: Install dependencies
        run: npm ci

      - name: Build static site
        run: |
          REPO="${{ github.event.repository.name }}"
          OWNER="${{ github.repository_owner }}"
          OWNER_LC=$(echo "$OWNER" | tr '[:upper:]' '[:lower:]')
          if [[ "$REPO" == *.github.io ]]; then
            export BASE_PATH=""
            export NEXT_PUBLIC_SITE_URL="https://${OWNER_LC}.github.io"
          else
            export BASE_PATH="/$REPO"
            export NEXT_PUBLIC_SITE_URL="https://${OWNER_LC}.github.io/$REPO"
          fi
          npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: out
          retention-days: 7

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v5
